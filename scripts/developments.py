
def execution():
    # res = fetch(node['type'])([(node['value'])  # return
    #                            # for all input nodes
    #                            for i in node['inputs']
    #                            # interate nodes to find input nodes
    #                            for node in nodes
    #                            # condition to find the input node
    #                            if i == node['label']

    #                            ])

    # print(getattr(__builtins__['int'], '__call__')('1'))
    # print([(node['label'].split('_')[0]).lower()  # return
    #        # for all input nodes
    #        for i in node['inputs']
    #        # interate nodes to find input nodes
    #        for node in nodes
    #        # condition to find the input node
    #        if i == node['label']

    #        ])
    # print([i for i in [(node['value'])  # return
    #        # for all input nodes
    #                    for i in node['inputs']
    #                    # interate nodes to find input nodes
    #                    for node in nodes
    #                    # condition to find the input node
    #                    if i == node['label']

    #                    ]])

    # print(
    #     [getattr(__builtins__[i[1].split('_')[0].lower()], '__call__')(i[0]) for i in [((node['value']), node['label'])  # return
    #      # for all input nodes
    #                                                                                    for i in node['inputs']
    #                                                                                    # interate nodes to find input nodes
    #                                                                                    for node in nodes
    #                                                                                    # condition to find the input node
    #                                                                                    if i == node['label']

    #                                                                                    ]]
    # )

    pass


def dynamic_output():
    # print([j['outputs']
    #               for j in nodes if j['label'] == (node['inputs'][0])])
    pass
